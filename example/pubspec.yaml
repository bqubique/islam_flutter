name: islam_flutter_example
description: Example app for islam_flutter package.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  islam_flutter:
    path: ../

flutter:
  uses-material-design: true
